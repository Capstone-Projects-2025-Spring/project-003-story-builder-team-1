import React from 'react';
import { Center } from '@mantine/core';

function HOME() {

    return (
        <Center h={150}>
            Click "Generate New Story" to begin writing a new story!
        </Center>
    )
};

export default HOME;
